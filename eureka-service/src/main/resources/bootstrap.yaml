spring:
  application:
    name: eureka-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
      fail-fast: true
      retry:
        maxAttempts: 200
        maxInterval: 10000
